"""The two workflows. They share primitives; they are not the same product.

**replicate** (W1) — a faithful, complete copy of an existing site into Builder,
routes preserved. A productized service, not a redesign tool.

**theme** (W2) — evolve a design system: token manifest, components, template,
data-driven pages.

Merging them is what produced the drift this project was restarted to avoid.
"""
