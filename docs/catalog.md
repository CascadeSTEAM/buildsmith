# Component catalog — `example`

**Generated by `buildsmith docs`. Do not edit.** The pre-commit hook regenerates
this and refuses the commit if it differs, so hand edits are lost and a stale
catalogue cannot be committed.

Built from `sites/example/design/`. Token references are shown as the logical
keys the specs use; the uuids they resolve to are per-site and live in that
site's applied token map.

## Design tokens

| key | type | value | dark | group |
|---|---|---|---|---|
| `brand-ink` | Color | `#12261f` | `#eef5f2` | brand |
| `brand-primary` | Color | `#0a7d55` | `#3fbf8c` | brand |
| `map-border` | Color | `#d8ded9` | `#2a332e` | map |
| `map-height` | Dimension | `320px` | — | map |
| `map-radius` | Dimension | `12px` | — | map |
| `map-width` | Dimension | `100%` | — | map |
| `radius-card` | Dimension | `12px` | — | shape |
| `space-gutter` | Dimension | `24px` | — | spacing |
| `space-section` | Dimension | `64px` | — | spacing |
| `surface` | Color | `#ffffff` | `#0e1512` | surface |

10 token(s). Only `Color` and `Dimension` exist — anything else
is a component prop plus one injected stylesheet (TRAP-004).

## Components

### `location-map` — Location Map

```
<iframe>
```

**Tokens consumed:** `map-border`, `map-height`, `map-radius`, `map-width`

### `site-footer` — Site Footer

```
<footer>
  <address> "1 Example Way, Nowhere"
```

**Tokens consumed:** `brand-ink`, `space-section`, `surface`

### `site-header` — Site Header

```
<header>
  <nav>
    <a> "Home"
    <a> "About"
```

**Tokens consumed:** `brand-ink`, `brand-primary`, `space-gutter`, `surface`

## Template

**Example Marketing Template** — route `template/example-marketing`

- `is_template`: yes
- `template_group`: _none_

## Pages

| title | route | dynamic |
|---|---|---|
| About | `about` | no |
